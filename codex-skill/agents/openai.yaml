interface:
  display_name: "Total Memory"
  short_description: "Persistent cross-session memory and self-improvement"

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "memory"
      description: "Total Memory MCP server — 20 tools for persistent knowledge storage, 4-tier search (keyword, semantic, fuzzy, graph), self-improving error-to-insight-to-rule pipeline, privacy stripping, branch-aware context, and observations tracking"
